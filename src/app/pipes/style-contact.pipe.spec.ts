import { StyleContactPipe } from './style-contact.pipe';

describe('StyleContactPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleContactPipe();
    expect(pipe).toBeTruthy();
  });
});
