import { RemoveTitleColumnPipe } from './remove-title-column.pipe';

describe('RemoveTitleColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTitleColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
