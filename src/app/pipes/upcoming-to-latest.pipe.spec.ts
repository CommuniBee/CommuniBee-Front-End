import { UpcomingToLatestPipe } from './upcoming-to-latest.pipe';

describe('UpcomingToLatestPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingToLatestPipe();
    expect(pipe).toBeTruthy();
  });
});
