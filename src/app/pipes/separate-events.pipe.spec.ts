import { SeparateEventsPipe } from './separate-events.pipe';

describe('SeparateEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
